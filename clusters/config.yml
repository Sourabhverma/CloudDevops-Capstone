apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: capstone
  region: ap-south-1

nodeGroups:
  - name: node-group-1 
    instanceType: t2.medium
    desiredCapacity: 2
    ssh:
      allow: false